#include <Windows.h>
#include <stdio.h>
#include "lznt1.h"

int main() {
	
	byte data[0x10] = { 0x90,0x40,0x5a,0x90,0x92,0x90,0x90,0x40,0x90,0x30,0x90,0x10,0x90,0x12,0x70,0x90, };
	
	void* buf = malloc(32); memset(buf, 0, 32);
	ssize_t len = lznt1_block(data, 16, 0, buf);

	len = lznt1_decompress(buf,len,data);

	printf("%d\n", len);
}